# Neon Postgres connection string for the development branch.
# Get this from Neon dashboard → your project → Connection details.
# Use the pooled connection string (ends with `?sslmode=require`) for app runtime.
# Migrations also use this URL; point it at the dev branch, not prod.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
